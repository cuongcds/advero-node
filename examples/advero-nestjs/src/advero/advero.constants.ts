/** DI token for the AdveroClient instance registered by AdveroModule. */
export const ADVERO_CLIENT = Symbol('ADVERO_CLIENT');

/** DI token for the AdveroModule's own options (baseUrl/apiKey/apiSecret/cache). */
export const ADVERO_MODULE_OPTIONS = Symbol('ADVERO_MODULE_OPTIONS');
